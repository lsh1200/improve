---
description: Run /improve with !verbose — shows the rewritten prompt in a fenced block before executing.
argument-hint: <raw prompt>
---

Invoke the `improve` skill via the Skill tool. Pass these arguments exactly, with `!verbose` prepended so the skill's Phase 2 rewrite is printed before execution:

!verbose $ARGUMENTS
